# LogbookCore

Legacy scaffold folder.

The actual build target lives in:

- `Sources/LogbookCore`

That target currently owns:

- activity models
- persistence
- capture settings
- session review models
- sessionization and privacy filtering
